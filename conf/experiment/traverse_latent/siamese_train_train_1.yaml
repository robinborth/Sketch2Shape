# @package _global_

defaults:
  - override /data: shapenet_chair_4096
  - override /logger: wandb

ckpt_path: ${paths.checkpoint_dir}/deepsdf.ckpt
loss_ckpt_path: ${paths.checkpoint_dir}/siamese.ckpt
tags: ["traverse_latent", "shapenet_chair_4096", "siamese_train_train_1"]
create_mesh: False
create_video: True
traversal_steps: 50

model:
  data_dir: ${data.data_dir}
  loss_ckpt_path: ${loss_ckpt_path}
  shape_k: 16
  shape_init: True
  compute_snn_loss: True
  prior_idx_start: 0 
  prior_idx_end: 3