# @package _global_

defaults:
  - override /data:
    - train_siamese
    - eval_siamese
  - override /dataset: shapenet_chair_4096
  - override /model: siamese
  - override /logger: wandb

ckpt_path: ${paths.checkpoint_dir}/siamese.ckpt
tags: ["eval_siamese", "shapenet_chair_4096", "siamese"]

