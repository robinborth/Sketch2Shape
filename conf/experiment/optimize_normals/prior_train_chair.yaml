# @package _global_

defaults:
  - override /data:
    - shapenet_chair_4096
    - optimize_latent
  - override /logger: wandb

ckpt_path: /home/borth/sketch2shape/checkpoints/deepsdf.ckpt 
tags: ["optimize_normals", "shapenet_chair_4096", "prior_train_chair"]
split: train 
prior_idx: prior 
obj_ids:
  - 1a6f615e8b1b5ae4dbbc9440457e303e

trainer:
  max_epochs: 20
  accumulate_grad_batches: 4

model:
  video_capture_rate: 8
  reg_weight: 1e-1
  optimizer:
    lr: 5e-3