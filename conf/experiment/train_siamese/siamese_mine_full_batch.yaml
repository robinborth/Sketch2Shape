# @package _global_

defaults:
  - data:
    - shapenet_chair_16
    - train_siamese
    - eval_siamese
  - override /logger: wandb

tags: ["train_siamese", "mine_full_batch"]

model:
  mine_full_batch: false
