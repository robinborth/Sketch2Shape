# @package _global_

defaults:
  - data:
    - shapenet_chair_16
    - train_siamese
    - eval_siamese
  - override /logger: wandb

tags: ["train_siamese", "type_of_triplets"]

model:
  miner:
    type_of_triplets: all 