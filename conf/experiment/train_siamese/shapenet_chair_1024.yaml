# @package _global_

defaults:
  - override /data:
    - shapenet_chair_1024
    - train_siamese
    - eval_siamese
  - override /logger: wandb

tags: ["train_siamese", "shapenet_chair_1024"]

trainer:
  max_epochs: 20
  val_check_interval: 0.25
  
callbacks:
  early_stopping:
    patience: 20
