# @package _global_

defaults:
  - override /data:
    - shapenet_chair_4096
    - optimize_latent
  - override /logger: wandb

ckpt_path: /home/borth/sketch2shape/checkpoints/deepsdf.ckpt 
siamese_ckpt_path: ${paths.checkpoint_dir}/siamese.ckpt
tags: ["optimize_sketch", "shapenet_chair_4096", "chair_1_train_mean"]
split: train 
prior_idx: mean 
obj_ids:
  - 353707e135c889d2dbbc9440457e303e

trainer:
  max_epochs: 20
  accumulate_grad_batches: 8

model:
  siamese_weight: 1.0
  video_capture_rate: 8
  reg_loss: False
  reg_weight: 1e-04
  optimizer:
    lr: 1e-02
  scheduler:
    step_size: 5
    gamma: 0.5