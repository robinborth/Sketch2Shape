# @package _global_

defaults:
  - override /data: optimize_latent
  - override /dataset: shapenet_chair_4096 
  - override /logger: wandb

tags: ["optimize_sketch", "shapenet_chair_4096", "optimize_silhouette"]
model:
  silhouette_loss: l1 
  silhouette_weight: 1.0
  reg_loss: latent
  reg_weight: 1.0