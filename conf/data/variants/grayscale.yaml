# @package _global_

data:
  dataset:
    normal_transform:
      transforms:
        - _target_: lib.data.transforms.ToGrayScale 

eval_data:
  dataset:
    normal_transform:
      transforms:
        - _target_: lib.data.transforms.ToGrayScale 
